interface:
  display_name: "장소·오퍼레이터·라이터 선정"
  short_description: "3.0 경로·라이터·등급별 실행과 수동 Git 흐름을 적용"
  default_prompt: "Use $location-operator-writer-routing for C:\\sswcenter\\3.0: verify the repo-bundled wrapper, DeepSeek runner, project-local skill, and fixed local auth path before applying the operator/writer and grade workflow."
