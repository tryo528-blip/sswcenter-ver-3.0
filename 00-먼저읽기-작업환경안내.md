# SSWCenter 3.0 작업환경 안내

> 지위: 장소·경로·로컬 비밀정보·Git 안전 경계의 정본
> 위치: `C:\sswcenter\3.0\00-먼저읽기-작업환경안내.md`
> 적용 기준: 2026-08-12

## 1. 현재 운영 방식

- 작업 장소는 `집(HOME)`과 `사무실(OFFICE)`이다.
- 두 장소의 제품 저장소 기준 경로는 `C:\sswcenter\3.0`이다.
- 미완료 작업을 다른 PC의 복사본에서 추정해 이어가지 않는다. 원래 PC의 task/thread와 worktree를 계속 사용한다.
- 백업 매체나 과거 저장소를 개발 작업본으로 사용하지 않는다.
- Git 동기화·커밋·통합·푸시는 형님의 명시 요청이 있을 때만 수행한다.

## 2. 표준 폴더 배치

```text
C:\sswcenter\
├─ 3.0\                         # 제품 Git 저장소 + pull 배포 운영도구
│  ├─ warpper\                  # 집·사무실 공통 AI 래퍼
│  │  ├─ invoke-grok.ps1
│  │  ├─ invoke-codex.ps1
│  │  ├─ invoke-opus.ps1
│  │  └─ wrapper-config.json
│  ├─ deepseek_runner\          # DeepSeek 최소 실행 묶음
│  │  ├─ .dsw-root
│  │  ├─ invoke-deepseek-writer.ps1
│  │  ├─ DeepSeekClient.psm1
│  │  ├─ EditBatch.psm1
│  │  └─ Workspace.psm1
│  ├─ .agents\skills\location-operator-writer\
│  │                              # Git pull로 자동 배포되는 프로젝트 스킬
│  └─ scripts\setup-local-ai-auth.ps1
├─ worktrees\3.0\              # Codex 독립 room worktree 슬롯
│  ├─ room-1\
│  ├─ room-2\
│  ├─ room-3\
│  ├─ room-4\
│  ├─ room-5\
│  ├─ room-6\
│  └─ room-7\                  # Opus 한도 초과 대체검수에 우선 사용
└─ api-keys.local.env          # 로컬 전용, Git 추적 금지
```

`warpper`는 현재 실제 폴더명이다. 실제 폴더를 바꾸기 전까지 임의로 `wrapper`로 고치지 않는다.

## 3. 고정 경로

```text
REPOSITORY_ROOT=C:\sswcenter\3.0
WRAPPER_PACKAGE=C:\sswcenter\3.0\warpper
WRAPPER_CONFIG=C:\sswcenter\3.0\warpper\wrapper-config.json
WORKTREE_PARENT=C:\sswcenter\worktrees\3.0
DEEPSEEK_RUNNER=C:\sswcenter\3.0\deepseek_runner\invoke-deepseek-writer.ps1
PROJECT_SKILL=C:\sswcenter\3.0\.agents\skills\location-operator-writer
API_ENV_FILE=C:\sswcenter\api-keys.local.env
```

집·사무실 공통 설치 경로:

| 항목 | 경로 |
|---|---|
| PowerShell 7 | `C:\tools\PowerShell7\pwsh.exe` |
| Grok 래퍼 | `C:\sswcenter\3.0\warpper\invoke-grok.ps1` |
| Grok CLI | `%USERPROFILE%\.grok\bin\grok.exe` |
| Codex 래퍼 | `C:\sswcenter\3.0\warpper\invoke-codex.ps1` |
| Codex CLI | `%LOCALAPPDATA%\OpenAI\Codex\bin\<16자리 해시>\codex.exe` |
| Opus 래퍼 | `C:\sswcenter\3.0\warpper\invoke-opus.ps1` |
| Claude CLI | `%APPDATA%\npm\node_modules\@anthropic-ai\claude-code\bin\claude.exe` |
| DeepSeek 러너 | `C:\sswcenter\3.0\deepseek_runner\invoke-deepseek-writer.ps1` |
| 프로젝트 스킬 | `C:\sswcenter\3.0\.agents\skills\location-operator-writer` |

`home`과 `office` profile은 같은 환경변수 경로 템플릿을 사용한다. 래퍼가 현재 PC의 `USERPROFILE`, `APPDATA`, `LOCALAPPDATA`만 확장하며 임의 PATH 탐색은 하지 않는다. Codex Desktop의 16자리 버전 폴더만 기존 제한된 자동 복구 규칙을 적용한다.

## 4. 작업 시작 전 읽기 전용 확인

```powershell
$Repo = 'C:\sswcenter\3.0'
$Wrapper = 'C:\sswcenter\3.0\warpper'
$Config = Join-Path $Wrapper 'wrapper-config.json'
$Runner = 'C:\sswcenter\3.0\deepseek_runner\invoke-deepseek-writer.ps1'
$KeyFile = 'C:\sswcenter\api-keys.local.env'

$gitTop = (& git -C $Repo rev-parse --show-toplevel 2>$null).Trim()
$branch = (& git -C $Repo branch --show-current 2>$null).Trim()
$status = @(& git -C $Repo status --short 2>$null)

[pscustomobject]@{
    GitTop = $gitTop
    Branch = $branch
    Changes = $status.Count
    ConfigExists = Test-Path -LiteralPath $Config -PathType Leaf
    RunnerExists = Test-Path -LiteralPath $Runner -PathType Leaf
    KeyFileExists = Test-Path -LiteralPath $KeyFile -PathType Leaf
}
```

- `GitTop`은 정확히 `C:\sswcenter\3.0`이어야 한다.
- 현재 브랜치와 변경 수는 보고하되 자동으로 전환·정리하지 않는다.
- wrapper의 활성 profile은 현재 장소와 일치해야 한다.
- wrapper의 저장소 루트가 3.0이 아니거나 필수 실행파일이 없으면 `CONFIG_DRIFT` 또는 `BLOCKED`로 보고한다.

## 5. DeepSeek와 인증

- DeepSeek 러너는 `C:\sswcenter\3.0\deepseek_runner\invoke-deepseek-writer.ps1`만 사용한다.
- 러너에 `-RepoRoot`, `-TaskPacketPath`, `-WriteAllowList`, `-ReadAllowList`, `-EnvFile`을 명시한다.
- `-EnvFile`은 `C:\sswcenter\api-keys.local.env`만 허용한다.
- 다른 키 파일을 검색하거나 `DEEPSEEK_API_KEY` 프로세스 환경변수로 fallback하지 않는다.
- 키 파일이 없거나 `DEEPSEEK_API_KEY` 항목이 정확히 하나가 아니면 호출하지 않는다.
- 키 값과 인증 파일 내용은 화면·로그·프롬프트·handoff·Git에 남기지 않는다.

각 PC 최초 1회에는 다음 명령으로 로컬 키를 안전하게 입력한다. 기존 파일은 `-Replace` 없이는 덮어쓰지 않는다.

```powershell
& 'C:\tools\PowerShell7\pwsh.exe' -NoProfile -File `
  'C:\sswcenter\3.0\scripts\setup-local-ai-auth.ps1'
```

검증만 할 때는 같은 명령에 `-ValidateOnly`를 붙인다.

## 6. Git 수동 운영

- 자동 `pull`, `stage`, `commit`, `cherry-pick`, `push`를 하지 않는다.
- 형님이 `커밋해`, `푸시해`, `통합해`, `pull해`처럼 명시했을 때만 요청 범위를 수행한다.
- 실행 전 대상 저장소·브랜치·변경사항·원격 상태를 확인하고 결과를 보고한다.
- `force push`, `reset --hard`, `clean`, 무단 rebase는 사용하지 않는다.
- 기존 변경이나 room worktree를 임의 삭제·덮어쓰기·재설정하지 않는다.

## 7. 2.2 경계

- 활성 저장소·wrapper·runner·인증 설정에서 `C:\sswcenter\2.2`를 사용하지 않는다.
- 3.0 내부의 과거 handoff/report에 남은 2.2 경로는 역사 기록일 뿐이다.
- 과거 문서의 2.2 명령을 복사해 실행하거나 해당 폴더를 현재 작업 근거로 탐색하지 않는다.

## 한 줄 기억법

> **제품은 3.0, DeepSeek는 외부 고정 러너, 인증은 고정 파일, Git 변경은 형님 요청 때만.**
