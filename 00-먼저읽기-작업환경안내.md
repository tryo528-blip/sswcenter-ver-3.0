# SSWCenter 3.0 작업환경 안내

> 지위: 실행 OS·고정 경로·인증·프로젝트 접근·Git 안전 경계의 정본
> 위치: `/home/codexctl/workspace/sswcenter-3-0/00-먼저읽기-작업환경안내.md`
> 적용 기준: 2026-08-16

## 1. 현재 운영 방식

- 작업 방식은 원격 전용 `REMOTE`다. 장소 선택값은 더 이상 사용하지 않는다.
- 실행 OS는 Ubuntu Linux이고, 제품 저장소는 `/home/codexctl/workspace/sswcenter-3-0` 하나만 사용한다.
- 형님은 Codex에게만 자연어로 지시한다. Codex가 현재 요청에 맞춰 Codex·Grok·DeepSeek 중 actor와 구현·검수·수정 action을 정한다.
- Windows의 AI 실행파일과 `C:\sswcenter`는 형님의 개인용 또는 삭제 대상일 뿐 이 프로젝트에서 호출하거나 정본으로 보지 않는다.
- Git 동기화·커밋·통합·푸시는 형님의 명시 요청이 있을 때만 Codex가 수행한다.

## 2. 고정 배치

```text
/home/codexctl/workspace/sswcenter-3-0/       # 유일한 제품 Git 저장소
├─ AGENTS.md                                  # 모든 actor가 읽는 프로젝트 규칙
├─ .grok/config.toml                          # Grok 권한·환경 정책
├─ .grok/sandbox.toml                         # Grok project-only sandbox
├─ review/                                    # 검증 원장·보고서·패킷
└─ scripts/                                   # 제품·검증용 스크립트

/home/codexctl/.local/bin/ssw-agent           # Codex가 호출하는 단일 project-agent 명령
/home/codexctl/.local/bin/grok                # 공식 Grok Linux CLI
/home/codexctl/.local/share/sswcenter-agent/  # 프로젝트 runner 코드·설치 증거
/home/codexctl/.grok/                         # Grok 월구독 로그인·세션 상태
/home/codexctl/.config/sswcenter-agent/       # DeepSeek 프로젝트 API 인증
```

Grok·DeepSeek 모델 자체는 외부 서비스다. 저장소 안에는 실행 규칙과 sandbox 정책만 있고, Linux 실행파일과 인증은 저장소 밖 `codexctl` 사용자 영역에 둔다. 두 provider는 이 Linux 저장소를 작업 대상으로 사용한다.

### 제품 개발·검증 런타임

2026-08-15 W0 봉인 기준의 고정 실행환경은 다음과 같다.

| 도구 | 버전 | 고정 위치·관리 방식 |
|---|---|---|
| Node.js / npm | `v24.19.0` / `11.17.0` | `/usr/local/bin/node`, `/usr/local/bin/npm` |
| Python | `3.12.3` | `scripts/ensure-runtime.ps1`가 `backend/requirements.lock`으로 동기화하는 ignored `backend/.venv` |
| uv | `0.12.3` | `/usr/local/bin/uv`; `backend/requirements.lock`의 hash를 적용 |
| PowerShell | `7.6.4` | `/home/codexctl/.local/bin/pwsh`; 사용자 영역 설치 |
| PostgreSQL client/server | `16.14` | `/usr/bin/psql`, `/usr/lib/postgresql/16/bin`; 임시 loopback cluster만 검증에 사용 |
| Playwright Chromium | `149.0.7827.55`, build `1228` | `/home/codexctl/.cache/ms-playwright`; 사용자 cache |
| Chromium 공유 라이브러리 | Ubuntu 24.04용 NSS·NSPR·ALSA | `/home/codexctl/.local/share/sswcenter-playwright-libs/ubuntu-24.04`; 시스템 package 영역을 변경하지 않음 |

PowerShell은 [공식 v7.6.4 Linux x64 release](https://github.com/PowerShell/PowerShell/releases/tag/v7.6.4)의 tarball만 사용하며, 설치 archive SHA-256은 `4471b5a36bfe86ec7af8525d36bb1cacba0128e7aac22d05cc064bc00e604721`로 확인했다. Python virtualenv, Playwright browser, Chromium 공유 라이브러리는 Git 대상이 아니며 재생성 가능한 사용자 실행환경이다. Python 의존성 정본은 hash가 포함된 `backend/requirements.lock`, frontend 의존성 정본은 `frontend/package-lock.json`이다.

실행 interpreter는 현재 Ubuntu 정본의 실제 설치 상태에 맞춰 Python `3.12.3`으로 고정한다. `backend/pyproject.toml`의 Ruff·mypy `py311` target은 배포 호환성 검사를 위한 정적 target이며 실행 interpreter 버전과 혼동하지 않는다. 모든 actor가 같은 ignored venv를 사용하도록 작업 시작 전에 아래 preflight를 실행한다. 기본은 네트워크를 사용하지 않는 offline lock sync다.

```bash
pwsh -NoProfile -File scripts/ensure-runtime.ps1
pwsh -NoProfile -File scripts/verify-runtime.ps1
```

`ensure-runtime.ps1 -Online`은 offline cache에 없는 패키지가 필요할 때만 형님 승인 후 사용한다. provider를 호출할 때도 `VIRTUAL_ENV=/home/codexctl/workspace/sswcenter-3-0/backend/.venv`와 해당 `bin`을 PATH 앞에 두고 같은 preflight 결과를 원장에 기록한다.

W0 봉인 검증 진입점은 두 개다.

```bash
pwsh -NoProfile -File scripts/test.ps1 -FoundationOnly
pwsh -NoProfile -File scripts/test-w0-postgres-linux.ps1
```

첫 명령은 OpenAPI·dependency lock·정적 검사·backend·frontend·build·W0 browser E2E를 실행한다. 두 번째 명령은 `/tmp/sswcenter-w0-pg-*` 아래에만 PostgreSQL cluster를 만들고 현재 head migration, postcheck, readiness write gate, application-role audit 불변성을 검사한 뒤 포트·process·임시파일을 0으로 정리한다.

## 3. 단일 실행 명령

Codex는 provider 작업에 아래 명령만 사용한다.

```text
/home/codexctl/.local/bin/ssw-agent
```

주요 진입점은 다음 네 개다.

```bash
ssw-agent status
ssw-agent self-test
ssw-agent auth grok
ssw-agent auth deepseek
```

- `status`: 경로, 설치, 인증 준비 여부, 정책 hash를 확인하되 비밀값은 읽거나 출력하지 않는다.
- `self-test`: 프로젝트 쓰기, 검수 read-only, `.git` 쓰기 차단, 시스템 쓰기 차단, credential 읽기 차단을 실제로 확인한다.
- `auth grok`: 원격 기기 인증으로 형님의 Grok 월구독 계정을 연결한다.
- `auth deepseek`: 터미널의 숨김 입력으로 프로젝트 전용 DeepSeek API 키를 mode `0600`에 저장한다.
- provider tool-turn 예산은 기본 `128`턴이며, 요청값은 최대 `256`턴까지 허용한다. 이 상한은
  DeepSeek tool-call loop와 Grok CLI `--max-turns`에 공통 적용한다.
- migration·PostgreSQL·동시성·복구·cross-layer처럼 오래 걸릴 가능성이 높은 범위는 처음부터
  `256`턴과 `timeout=3600초`를 사용한다. Grok 모델은 월구독 계정의 현재 CLI 기본 모델을
  사용하고 모델 ID를 강제로 고정하지 않으며, reasoning effort는 runner가 항상 명시적으로
  `xhigh`를 전달한다. 2026-08-16 확인 시 CLI 기본 모델은 `grok-4.6`이다.

형님은 이 명령 형식을 외울 필요가 없다. 실제 작업과 provider 호출은 Codex가 수행한다.

## 4. 프로젝트 접근 경계

| 실행 | 프로젝트 파일 | 테스트·명령 | 프로젝트 밖 | `.git` |
|---|---|---|---|---|
| Codex | 요청 범위에서 읽기·쓰기 | 가능 | 필요한 운영 확인만 | 쓰기는 형님 명시 요청 때만 |
| Grok `IMPLEMENT`·`FIX` | 전체 읽기·쓰기 | sandbox 안에서 가능 | 시스템 경로 read-only, 다른 사용자 파일 차단 | read-only |
| Grok `REVIEW` | 전체 읽기 | read-only 범위에서 가능 | 시스템 경로 read-only, 다른 사용자 파일 차단 | read-only |
| DeepSeek `IMPLEMENT`·`FIX` | 경로 검증 도구로 전체 읽기·쓰기 | `bubblewrap` 안에서 가능 | 사용자 홈·Windows 경로·credential 차단, 시스템 read-only | read-only |
| DeepSeek `REVIEW` | 전체 읽기 | 저장소 전체 read-only | 사용자 홈·Windows 경로·credential 차단, 시스템 read-only | read-only |

`프로젝트 전체 접근`은 제품 파일을 읽고 구현·수정·삭제하며 필요한 테스트를 실행할 수 있다는 뜻이다. Git 제어영역 `.git`, provider credential, Linux 시스템 파일은 제품 내용이 아니므로 별도 보호한다.

## 5. 인증 경계

- Grok 인증 방식은 API 키가 아니라 형님의 월구독 계정에 대한 공식 CLI device authentication이다.
- DeepSeek 인증 방식은 프로젝트 전용 API 키다.
- 인증 파일은 저장소 밖에 있고 Git에 포함되지 않는다.
- Codex는 인증 파일의 존재·권한만 확인한다. 키·토큰·쿠키 내용을 읽거나 화면·로그·prompt·보고서에 출력하지 않는다.
- DeepSeek API 키는 DeepSeek의 command sandbox에 전달하지 않는다.
- Grok CLI 로그인 상태와 Windows의 개인용 Grok 설치는 서로 별개다.

## 6. 작업 시작 확인

```bash
git -C /home/codexctl/workspace/sswcenter-3-0 rev-parse --show-toplevel
git -C /home/codexctl/workspace/sswcenter-3-0 branch --show-current
git -C /home/codexctl/workspace/sswcenter-3-0 status --short
ssw-agent status
```

- Git 최상위는 정확히 `/home/codexctl/workspace/sswcenter-3-0`이어야 한다.
- 현재 branch와 변경사항을 보고하되 자동으로 전환·정리하지 않는다.
- actor와 action은 현재 자연어 요청마다 새로 정한다. 장소 선택값이나 고정 writer를 복원하지 않는다.
- 고정 root, policy hash, sandbox, provider 인증이 맞지 않으면 원인을 보고하고 해당 provider 실행을 중단한다.

## 7. Git 수동 운영

- 자동 `pull`, `stage`, `commit`, `cherry-pick`, `push`, `checkout`, `switch`, `reset`, `clean`, `rebase`, `merge`를 하지 않는다.
- 형님이 해당 동작을 명시했을 때만 Codex가 대상 저장소·branch·변경사항·원격 상태를 확인하고 요청 범위만 수행한다.
- Grok·DeepSeek는 작업 파일을 수정할 수 있지만 `.git`은 수정할 수 없다.
- 기존 변경이나 worktree를 임의 삭제·덮어쓰기·재설정하지 않는다.

## 8. 폐기된 실행 경로

- 과거 Windows AI 실행 파일과 프로젝트 skill은 현재 tree에서 제거했으며 Git 이력에서만 복구할 수 있다.
- 장소 선택 호출문, Windows PowerShell provider wrapper, Windows provider CLI, `C:\sswcenter\2.x`·Linux 2.x 저장소를 현재 작업에 사용하지 않는다.
- 과거 handoff·report에 남은 위 문자열은 역사 기록일 뿐 현재 명령이나 정책 근거가 아니다.

## 한 줄 기억법

> **형님은 Codex에게만 말하고, 실제 작업은 Linux 저장소 하나에서 Codex·Grok·DeepSeek가 요청마다 역할을 바꿔 수행한다.**
