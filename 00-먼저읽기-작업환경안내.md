# SSWCenter 3.0 작업환경 안내

> 지위: 장소·경로·로컬 비밀정보·Git 안전 경계의 정본
> 위치: `C:\sswcenter\3.0\00-먼저읽기-작업환경안내.md`
> 적용 기준: 2026-08-15

## 1. 현재 운영 방식

- 작업 장소는 `집(HOME)`과 `사무실(OFFICE)`이다.
- 두 장소의 제품 저장소 기준 경로는 `C:\sswcenter\3.0`이다.
- 미완료 작업을 다른 PC의 복사본에서 추정해 이어가지 않는다. 원래 PC의 task/thread와 worktree를 계속 사용한다.
- 백업 매체나 과거 저장소를 개발 작업본으로 사용하지 않는다.
- Git 동기화·커밋·통합·푸시는 형님의 명시 요청이 있을 때만 수행한다.

## 2. 표준 폴더 배치

```text
C:\sswcenter\
├─ 3.0\                         # 제품 Git 저장소
│  ├─ .agents\skills\           # 프로젝트 운영 스킬
│  ├─ review\                    # 검증 원장·보고서·패킷
│  └─ scripts\                  # 제품·검증용 스크립트
└─ worktrees\3.0\              # 형님이 승인한 독립 작업방
```

저장소 안의 과거 `warpper`·`deepseek_runner`가 남아 있더라도 현재 호출 경로가 아니다. 이 legacy 묶음과 provider CLI는 실행하지 않고, 필요하면 역사 보존 자료로만 읽는다. Codex Desktop이 만든 독립방 worktree는 앱이 배정한 경로를 사용하며 임의로 복사해 방을 흉내 내지 않는다.

## 3. 고정 경로와 실행 경로

```text
REPOSITORY_ROOT=C:\sswcenter\3.0
MANUAL_WORKTREE_PARENT=C:\sswcenter\worktrees\3.0
CODEX_ROOM_WORKTREE=<Codex Desktop이 해당 task/thread에 배정한 절대 경로>
PROJECT_SKILL=C:\sswcenter\3.0\.agents\skills\location-operator-writer
SECURE_AI_WORKBENCH=<설치된 secure-ai-workbench 스킬의 검증 진입점>
```

활성 작업에 필요한 실행 경로는 세션 preflight에서 확인한다. 설치 위치·사용자명·인증 홈을 이 문서에 기기 고정으로 기록하지 않는다.

| 항목 | 경로·확인 방법 |
|---|---|
| PowerShell 7 | preflight에서 실제 실행 파일을 확인 |
| Codex 독립방 | task/thread에 배정된 worktree |
| Secure AI Workbench | 설치된 `secure-ai-workbench` 스킬과 검증 진입점 |
| 프로젝트 스킬 | `C:\sswcenter\3.0\.agents\skills\location-operator-writer` |

`HOME`과 `OFFICE`는 장소 선택값일 뿐 provider 인증·설치 경로를 추론하는 근거가 아니다. 외부 provider 호출은 Secure AI Workbench의 검증 진입점만 사용하며, provider에는 경로·URL·셸·도구·credential을 보내지 않는다.

## 4. 본진·독립방 작업 시작 전 읽기 전용 확인

```powershell
$Repo = 'C:\sswcenter\3.0'

$gitTop = (& git -C $Repo rev-parse --show-toplevel 2>$null).Trim()
$branch = (& git -C $Repo branch --show-current 2>$null).Trim()
$status = @(& git -C $Repo status --short 2>$null)

[pscustomobject]@{
    GitTop = $gitTop
    Branch = $branch
    Changes = $status.Count
}
```

- 본진의 `GitTop`은 정확히 `C:\sswcenter\3.0`이어야 한다. 독립방에서는 해당 task/thread에 배정된 worktree와 정확히 일치하는지 확인한다.
- 현재 브랜치와 변경 수는 보고하되 자동으로 전환·정리하지 않는다.
- 외부 provider를 호출할 때는 현재 장소·provider·오퍼레이터를 명시하고 Secure AI Workbench preflight를 통과해야 한다.
- repository root·route·Workbench 계약이 맞지 않으면 `CONFIG_DRIFT`, 실행환경이 없으면 `BLOCKED`로 보고한다.

## 5. Secure AI Workbench와 인증

- Grok·DeepSeek·Claude의 외부 호출은 설치된 `secure-ai-workbench` 스킬의 검증 진입점만 사용한다. legacy wrapper·runner·provider CLI는 사용하지 않는다.
- provider로 보낼 정확한 텍스트와 목적을 먼저 형님께 설명하고, 형님의 명시 승인을 받은 뒤 한 개의 schema `2.0` packet만 보낸다.
- packet에는 저장소 경로·URL·헤더·credential·셸·도구·attachment를 넣지 않는다. provider는 텍스트 조언만 반환하며 저장소를 직접 수정하지 않는다.
- provider credential은 설치된 `Set-AiWorkbenchKey.ps1 -Provider <provider>`의 대화형 설정만 사용한다. credential 파일을 읽거나 출력·복사·커밋하지 않는다.
- Workbench의 preflight·postflight·result schema·error code를 통과하지 못하면 호출 결과를 폐기하고 `BLOCKED` 또는 `CONFIG_DRIFT`로 보고한다.

키 값과 인증 파일 내용은 검증·진단·handoff에도 남기지 않는다.

## 6. Git 수동 운영

- 자동 `pull`, `stage`, `commit`, `cherry-pick`, `push`를 하지 않는다.
- 형님이 `커밋해`, `푸시해`, `통합해`, `pull해`처럼 명시했을 때만 요청 범위를 수행한다.
- 실행 전 대상 저장소·브랜치·변경사항·원격 상태를 확인하고 결과를 보고한다.
- `force push`, `reset --hard`, `clean`, 무단 rebase는 사용하지 않는다.
- 기존 변경이나 room worktree를 임의 삭제·덮어쓰기·재설정하지 않는다.

## 7. 2.2 경계

- 활성 저장소·설정에서 `C:\sswcenter\2.2`를 사용하지 않는다.
- 3.0 내부의 과거 handoff/report에 남은 2.2 경로는 역사 기록일 뿐이다.
- 과거 문서의 2.2 명령을 복사해 실행하거나 해당 폴더를 현재 작업 근거로 탐색하지 않는다.

## 한 줄 기억법

> **제품은 3.0, 방은 배정된 worktree, 외부 provider는 승인된 Secure AI Workbench 텍스트 경로, Git 변경은 형님 요청 때만.**
