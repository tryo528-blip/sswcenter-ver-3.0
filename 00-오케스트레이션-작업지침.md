# SSWCenter 3.0 AI 오케스트레이션·검증 지침

> 지위: AI 선택, 역할 전환, 프로젝트 실행, 독립 검증, prompt와 보고 정책의 정본
> 위치: `/home/codexctl/workspace/sswcenter-3-0/00-오케스트레이션-작업지침.md`
> 적용 기준: 2026-08-15

이 문서는 **형님의 자연어 지시를 누가 어떤 역할로 수행하는지**를 정한다. 실행 OS·경로·인증·Git 경계는 [`00-먼저읽기-작업환경안내.md`](00-먼저읽기-작업환경안내.md), 테스트·검수 등급의 의미는 [`docs/운영_테스트_검수_등급_정의_v1.1.md`](docs/운영_테스트_검수_등급_정의_v1.1.md), 실제 provider 명령과 결과 형식은 [`docs/AI_프로젝트_에이전트_실행계약_v1.0.md`](docs/AI_프로젝트_에이전트_실행계약_v1.0.md)가 각각 소유한다.

## 1. 요청 처리 흐름

```text
형님의 자연어 지시
  → Codex가 범위·완료조건 확인
  → ACTOR 선택
  → ACTION 선택
  → Linux 저장소에서 실행
  → 실제 diff·테스트·검수 확인
  → Codex가 형님께 최종 보고
```

- `ACTOR`: `CODEX`, `GROK`, `DEEPSEEK`
- `ACTION`: `IMPLEMENT`, `REVIEW`, `FIX`
- 형님이 actor나 action을 지정하면 그대로 따른다.
- 지정하지 않으면 Codex가 작업 난이도, 독립성, 비용·가용성, 필요한 도구를 보고 선택한다.
- 선택은 요청 한 건에만 유효하다. 같은 actor가 다음 요청에서 다른 action을 맡아도 되고, 이전 역할을 고정값으로 기억하지 않는다.
- 장소·오퍼레이터·라이터를 조합한 특수 호출문, 호출 별칭, `!` 접미사, 고정 writer는 사용하지 않는다.

## 2. Actor와 action

| actor | `IMPLEMENT` | `REVIEW` | `FIX` |
|---|---|---|---|
| `CODEX` | 범위 안에서 직접 구현·테스트 | 현재 바이트를 read-only로 검수 | 원인 진단 후 직접 수정·검증 |
| `GROK` | 공식 Linux CLI가 저장소를 읽고 구현·테스트 | 전체 실행을 read-only로 제한해 검수 | 저장소를 직접 수정하고 테스트 |
| `DEEPSEEK` | 공식 API tool calls로 저장소를 읽고 구현·테스트 | 쓰기 도구 제거와 read-only sandbox로 검수 | 저장소를 직접 수정하고 테스트 |

누가 맡았는지는 품질 등급이 아니다. 같은 완료조건과 실제 증거로 판정한다.

## 3. 단일 사용자 창구

- 형님은 모든 지시를 Codex에게만 한다.
- Codex는 actor 선택, provider 호출, 결과 수신, 현재 바이트 대조, 필요한 추가 테스트와 최종 보고를 담당한다.
- 형님의 현재 작업 지시는 그 범위 안에서 필요한 provider 실행을 포함한다. 별도 호출문 문법이나 매번 동일한 승인문을 요구하지 않는다.
- 다만 provider에 보내는 범위가 형님의 요청을 실질적으로 넓히거나 credential·개인정보·프로젝트 밖 자료가 필요해지면 진행하지 않고 형님께 설명한다.
- Grok·DeepSeek의 출력은 완료 증거가 아니다. Codex가 diff, 파일, 명령 exit, 테스트 결과를 확인해야 한다.

## 4. Provider 실행

- Grok과 DeepSeek는 `/home/codexctl/.local/bin/ssw-agent`를 통해서만 실행한다.
- Grok은 형님의 월구독 계정으로 로그인한 공식 Linux Grok CLI를 사용한다.
- DeepSeek는 프로젝트 전용 API 키로 공식 API를 사용한다.
- 두 provider 모두 현재 요청에 필요한 프로젝트 내용을 직접 읽을 수 있다. `IMPLEMENT`·`FIX`에서는 프로젝트 파일을 수정할 수 있다.
- `REVIEW` 실행에는 쓰기 권한이 없다. 검수 중 발견한 수정안을 직접 반영하지 않고 finding으로 반환한다.
- provider는 프로젝트 밖 사용자 홈, Windows 파일, credential을 읽을 수 없고 Linux 시스템을 수정할 수 없다.
- `.git`은 모든 provider 실행에서 read-only다. commit·push 등 Git 변경은 형님 요청을 받은 Codex만 수행한다.
- 과거 프로젝트 스킬, Windows wrapper, legacy runner를 fallback으로 호출하지 않는다.

## 5. 독립 검증과 등급

검증 등급 1~5는 작업 깊이를 정하는 선택지이며 특정 AI에 고정되지 않는다. 필요한 등급만 사용한다.

| 범위 | 대표 목적 |
|---|---|
| Grade 1~2 | 문법·정적 확인·단일 smoke·빠른 정확성 검수 |
| Grade 3~4 | unit·component·contract·cross-layer·architecture·security |
| Grade 5 | PostgreSQL·migration lifecycle·복구·adversarial·운영 경계 |

- 구현자와 독립 검수자를 분리해야 하는 작업은 다른 실행 단위로 나눈다.
- 같은 actor를 다시 쓰더라도 새 `REVIEW` 실행은 read-only이고 이전 실행의 자기보고를 그대로 신뢰하지 않는다.
- 테스트 담당은 요구를 약화하거나 테스트를 삭제·skip·xfail해 PASS를 만들지 않는다.
- static PASS나 provider의 `DONE`은 production acceptance·release 승인과 다르다.
- 최종 acceptance는 현재 바이트와 필요한 검증 증거를 본 Codex 또는 형님이 판정한다.

## 6. 실행 순서

1. 고정 Linux Git root, branch, status, 기존 변경을 확인한다.
2. 목표, 읽기 범위, 쓰기 범위, 완료조건, 금지사항을 한 번씩 명확히 정한다.
3. 현재 요청의 actor와 action을 선택한다.
4. Codex는 직접 작업하거나 `ssw-agent`로 한 번의 fresh provider 실행을 시작한다.
5. 실제 변경 경로와 diff를 확인한다. 검수 실행이면 쓰기가 없었는지 확인한다.
6. 위험에 비례한 테스트와 필요한 독립 검수를 수행한다.
7. 결과, actor/action, 변경, 테스트, finding, 미검증 항목, Git 수행 여부를 보고한다.

provider 실패를 성공이나 부분 성공으로 낮추지 않는다. 같은 목표 범위 안에서 Codex가 직접 이어가거나 다른 actor로 전환할 수 있지만, 전환 사실과 어느 결과를 채택했는지 최종 보고에 남긴다.

## 7. Prompt 원칙

- 한 prompt에는 action, 목표, 대상 범위, 완료조건, 금지사항, 출력 형식을 각각 한 번만 적는다.
- 현재 저장소를 먼저 읽고 기존 변경을 보존하도록 지시한다.
- 모델명·인증·sandbox 경로는 runtime이 소유하므로 작업 prompt에 반복하지 않는다.
- 구현 prompt는 테스트와 실제 변경 경로 보고를 요구한다.
- 검수 prompt는 read-only finding, 근거, 심각도, 미검증 범위를 요구한다.
- 지시를 바꿀 때는 새 지시가 이전 지시를 대체한다고 명확히 한다.
- credential, token, cookie, 인증 파일 내용은 prompt에 넣지 않는다.

## 8. 보고 형식

실행 전 선택은 짧게 알린다.

```text
ROOT=/home/codexctl/workspace/sswcenter-3-0
ACTOR=CODEX|GROK|DEEPSEEK
ACTION=IMPLEMENT|REVIEW|FIX
SCOPE=<현재 작업>
GIT=MANUAL_ONLY
```

종료 시에는 실제 결과만 적는다.

```text
STATUS=PASS|FAIL|BLOCKED
ACTOR=<실제 수행 actor>
ACTION=<실제 수행 action>
CHANGED_PATHS=<실제 변경 경로 또는 NONE>
TEST=<명령과 결과>
REVIEW=<finding과 근거 또는 NONE>
GIT=NOT_REQUESTED|REQUESTED_RESULT
UNVERIFIED=<남은 항목 또는 NONE>
```
