# SSWCenter 3.0 AI 오케스트레이션·검증 지침

> 지위: AI 역할·작업방·테스트·검수·최종검수 정책의 정본
> 위치: `C:\sswcenter\3.0\00-오케스트레이션-작업지침.md`
> 적용 기준: 2026-08-12

이 문서는 사람이 확인할 정책 정본이다. `$location-operator-writer <장소>-<오퍼레이터>-<라이터>!` 스킬은 이 정책을 실제 경로와 호출로 집행하는 실행 어댑터다. `$` 없이 8개 조합 중 하나인 `사무실-코덱스-그록!` 또는 `집-클로드-딥시크!`처럼 라우트만 입력해도 호출로 인식하며, `!`를 제거한 세 토큰으로 조합을 먼저 확정한다. 둘이 다르면 임의로 절충하지 않고 `CONFIG_DRIFT`로 중단한다.

## 1. 작업 시작 선택

```text
장소 → 저장소·도구 경로 → 오퍼레이터 → 라이터 → 테스트 등급 → 검수 등급 → 작업방 → 실행
```

- 장소: `HOME` 또는 `OFFICE`
- 오퍼레이터: `Codex` 또는 `Claude Code`
- 라이터: `Grok` 또는 `DeepSeek`

형님이 조합을 명시하면 그대로 사용한다. 빠진 선택이 작업 결과를 바꿀 때만 질문하고, 과거 세션 선택을 새 세션에 자동 승계하지 않는다.

## 2. 역할

| 역할 | 담당 | 책임 | 금지 |
|---|---|---|---|
| 최종 선택권자 | 형님 | 장소·역할·예외·Git 작업 여부 결정 | 이전 선택을 영구 위임으로 간주하지 않음 |
| 오퍼레이터 | Codex / Claude Code | 슬라이스 분해, packet 작성, 라이터 호출, diff 확인, 테스트·검수 배정, 최종 보고 | 독립검수를 자기 판단으로 대체 |
| 라이터 | Grok / DeepSeek | 승인 범위 구현·수정·리팩터링 | 허용 경로 밖 수정, Git 조작, 자기 결과 최종 PASS 판정 |
| 테스트 담당 | 등급별 모델 | 실행 가능한 테스트와 계약 검증 | 제품 요구를 약화해 PASS 생성 |
| 검수 담당 | 등급별 모델 | 변경과 증거를 read-only 검수 | 파일 수정 |
| 최종검수자 | Opus 4.6 또는 승인된 대체 | 슬라이스 전체 최종 독립 판정 | 구현·장기 대화형 수정 |

`DeepSeek Writer`는 별도 모델명이 아니라 `C:\sswcenter\3.0\deepseek_runner\invoke-deepseek-writer.ps1`로 DeepSeek를 Writer 역할에 사용하는 것을 뜻한다.

## 3. 라이터 권한

- 라이터는 packet에 명시된 쓰기 허용 경로만 수정한다.
- 저장소 전체 쓰기 권한은 기본적으로 금지한다.
- 테스트 담당자가 테스트 자체의 결함을 수정하려면 최소 재현과 승인된 테스트 경로가 필요하다.
- assertion 약화나 테스트 삭제로 PASS를 만들지 않는다.
- 검수 담당과 Opus 최종검수는 항상 read-only다.

## 4. 테스트·검수 등급

등급의 의미와 대표 범위는 [`docs/운영_오퍼레이터_등급_정의_v1.0.md`](docs/운영_오퍼레이터_등급_정의_v1.0.md)에서 읽고, 모델 배정은 아래 표를 사용한다.

| 용도 | 등급 | 모델 | effort | fast mode |
|---|---:|---|---|---|
| 테스트 | 1~2 | `gpt-5.3-codex-spark` | `xhigh` | off |
| 테스트 | 3~4 | `gpt-5.6-luna` | `max` | on |
| 테스트 | 5 | `gpt-5.6-sol` | `max` | off |
| 검수 | 1~2 | `gpt-5.6-luna` | `max` | on |
| 검수 | 3~4 | `gpt-5.6-sol` | `xhigh` | off |
| 검수 | 5 | `gpt-5.6-sol` | `ultra` | off |

- 테스트와 검수 등급은 따로 정한다.
- handoff의 오래된 모델표를 정본으로 사용하지 않는다.
- 실행 전 session settings 또는 래퍼 분기에서 실제 모델·effort·fast를 확인한다.
- 표와 실제 설정이 다르면 `CONFIG_DRIFT`로 중단한다.

## 5. Codex 오퍼레이터의 7개 독립 작업방

- `room-1`부터 `room-7`까지 각기 다른 task/thread와 Git worktree를 사용한다.
- 같은 worktree를 두 방에서 공유하지 않는다.
- 기존 room 경로를 삭제·덮어쓰기·reset하지 않는다.
- 제품 구현은 독립 room에서 수행하고 `C:\sswcenter\3.0`의 기존 변경을 보존한다.
- `room-7`은 Opus 한도 초과 대체검수에 우선 사용할 수 있도록 독립 상태를 유지한다.
- 완료된 task/thread는 handoff를 남기고 보관한다.
- worktree 삭제·정리는 형님의 명시 요청 없이는 수행하지 않는다.

Claude Code 오퍼레이터는 7개 room을 자동 상속하지 않는다. 형님이 별도로 지시했을 때만 적용한다.

## 6. 슬라이스 실행 순서

1. 장소·오퍼레이터·라이터와 정확한 저장소·room 루트를 확정한다.
2. 목표, 읽기 범위, 쓰기 허용 경로, 금지사항, 완료조건, 테스트·검수 등급을 packet에 봉인한다.
3. 선택한 라이터를 한 번 실행한다.
4. 라이터 보고가 아니라 실제 diff와 변경 경로를 확인한다.
5. 지정 등급으로 테스트한다.
6. 지정 등급으로 read-only 검수한다.
7. 수정이 필요하면 같은 라이터에 누락 범위만 재작업시키거나 형님께 전환 결정을 요청한다.
8. 최종검수와 검수 Grade 5에 Opus 규칙을 적용한다.
9. PASS/FAIL/BLOCKED, 실행 명령, 변경 경로, 테스트·검수 결과, 미검증 항목을 handoff에 기록한다.
10. Git 커밋·통합·푸시는 형님이 별도로 요청한 경우에만 수행한다.

## 7. DeepSeek Writer 계약

DeepSeek Writer는 자연어 prompt를 직접 받지 않고 JSON Task Packet을 받는다.

```powershell
& 'C:\tools\PowerShell7\pwsh.exe' -NoProfile -File `
  'C:\sswcenter\3.0\deepseek_runner\invoke-deepseek-writer.ps1' `
  -RepoRoot $roomRoot `
  -TaskPacketPath $taskPacketPath `
  -WriteAllowList $writeAllowPath `
  -ReadAllowList $readAllowPath `
  -EnvFile 'C:\sswcenter\api-keys.local.env'
```

- `RepoRoot`는 `C:\sswcenter\3.0` 또는 승인된 3.0 room worktree다.
- Task Packet의 `write_paths`와 `WriteAllowList`는 일치해야 한다.
- 인증 파일은 고정 경로만 허용한다. 키 자동 탐색과 환경변수 fallback을 사용하지 않는다.
- 키 값을 명령행·프롬프트·로그·보고서에 넣지 않는다.

## 8. Opus 최종검수

- `Claude Opus 4.6`
- 모델은 `claude-opus-4-6`으로 고정하고 `[1m]` 변형은 사용하지 않음
- `effort=max`
- 표준 컨텍스트만 사용: `CLAUDE_CODE_DISABLE_1M_CONTEXT=1`
- Extended/Adaptive Thinking 비활성: `CLAUDE_CODE_DISABLE_THINKING=1`
- `alwaysThinkingEnabled` 설정을 전달하지 않음
- 검수마다 새 비대화 세션
- 자료·기준·출력 형식을 한 번에 제공
- `--resume`, `--continue` 금지
- 같은 검수 재시도 최대 1회
- 검수는 read-only

### Opus 한도 초과 대체

1. 독립된 `room-7`을 사용한다.
2. Opus에 줄 동일 자료·기준·handoff를 한 번에 전달한다.
3. `gpt-5.6-sol`, `effort=ultra`, `fast=off`로 read-only 대체검수한다.
4. `OPUS_FALLBACK=CODEX_SOL_GRADE_5`와 Opus 미실행 사유를 기록한다.
5. 이 결과를 Opus PASS라고 쓰지 않는다.

## 9. Git 수동 승인 경계

- read-only 상태 확인은 허용한다.
- `pull`, `stage`, `commit`, `cherry-pick`, `push`, `checkout`, `reset`, `clean`, rebase는 자동 실행하지 않는다.
- 형님이 명시적으로 요청한 경우에만 대상 브랜치·커밋·diff를 확인한 뒤 요청 범위만 수행한다.
- cherry-pick이 필요한 통합 요청은 적용할 커밋과 대상 브랜치를 먼저 설명한다.
- force push, hard reset, clean, 무단 rebase는 사용하지 않는다.
- 현재 브랜치와 기존 변경사항은 그대로 보존한다.

## 10. 실패와 보고

- Writer의 `DONE`보다 실제 diff·완료조건·테스트 결과를 우선한다.
- Writer가 `PARTIAL`, `BLOCKED`, `FAIL`이면 실제 변경 여부와 원인을 먼저 확인한다.
- 다른 Writer 전환, 등급 상향, 범위 확대를 자동 수행하지 않는다.
- 검수 FAIL을 구현 FAIL로 자동 해석하지 않는다.
- 허용 경로 밖 변경, Git metadata 오염, 다른 장소 설정 변경을 발견하면 중단한다.

실행 전 보고:

```text
PLACE=OFFICE
OPERATOR=CODEX
WRITER=DEEPSEEK
REPOSITORY_ROOT=C:\sswcenter\3.0
ROOM=<선택한 독립 작업방>
TEST=<등급·모델·effort·fast>
REVIEW=<등급·모델·effort·fast>
FINAL=Opus 4.6 / max / standard context / thinking off
GIT=MANUAL_ONLY
```

종료 보고:

```text
STATUS=PASS|FAIL|BLOCKED
SLICE=<식별자>
CHANGED_PATHS=<실제 변경 경로>
TEST=<등급·명령·결과>
REVIEW=<등급·결과>
FINAL=<Opus 또는 명시된 fallback 결과>
GIT=NOT_REQUESTED|REQUESTED_RESULT
UNVERIFIED=<남은 항목>
```
