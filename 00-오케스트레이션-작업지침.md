# SSWCenter 3.0 AI 오케스트레이션·검증 지침

> 지위: AI 역할, 호출 경계, 독립 작업방, 검증 등급, 프롬프트와 보고 정책의 정본
> 위치: `C:\sswcenter\3.0\00-오케스트레이션-작업지침.md`
> 적용 기준: 2026-08-15

이 문서는 **누가 누구를 호출하고 어디서 작업하는지**만 정한다. 장소·경로·인증·Git 경계는 [`00-먼저읽기-작업환경안내.md`](00-먼저읽기-작업환경안내.md), 등급의 의미는 [`docs/운영_오퍼레이터_등급_정의_v1.0.md`](docs/운영_오퍼레이터_등급_정의_v1.0.md), 실제 Secure AI Workbench packet·result 형식은 [런타임 계약](.agents/skills/location-operator-writer/references/runtime-contracts.md)이 각각 소유한다. 다른 문서에는 이 내용을 복사하지 않고 링크만 둔다.

## 1. 작업 시작 선택

```text
장소 → 오퍼레이터 → 라이터(선택) → 구현 작업공간 → 테스트 등급 → 검수 등급 → 실행
```

- 장소: `HOME` 또는 `OFFICE`
- 오퍼레이터: `CODEX` 또는 `CLAUDE_CODE`
- 라이터: 필요할 때만 `GROK` 또는 `DEEPSEEK`

호출명 `<장소>-<오퍼레이터>-<라이터>!`의 세 토큰이 외부 라이터 경로를 선택한다. 외부 호출이 없는 Codex 작업은 라이터를 추론하지 않는다. 누락되거나 모호한 route를 기본값으로 채우지 않는다.

## 2. 역할과 호출 경계

| 선택 | 수행 방식 | 허용된 호출 | 금지 |
|---|---|---|---|
| `OPERATOR=CODEX` | Codex가 승인된 worktree에서 구현·테스트·read-only 검수를 직접 수행한다. | 필요할 때 Secure AI Workbench 텍스트 라이터·slice reviewer | provider의 프로젝트 접근·직접 수정, 자식 Codex 재호출 |
| `OPERATOR=CLAUDE_CODE` | Claude Code는 **명시된 한 슬라이스의 read-only 검수자**로만 참여한다. | 승인된 텍스트 검수 결과 반환 | 구현·Writer 호출·파일 수정·최종 acceptance 선언 |
| `WRITER=GROK` | 현재 오퍼레이터가 승인된 텍스트만 Workbench로 보낸다. | Secure AI Workbench의 `grok` provider | legacy wrapper·Grok CLI·저장소 직접 접근 |
| `WRITER=DEEPSEEK` | 현재 오퍼레이터가 승인된 schema `2.0` packet을 Workbench로 보낸다. | Secure AI Workbench의 `deepseek` provider | legacy runner·자연어만 전송·저장소 전체 쓰기 |
| 외부 `CLAUDE` | 한 슬라이스의 독립 텍스트 검수 의견만 반환한다. | Secure AI Workbench의 `claude` provider | 최종검수자 승격·구현·파일 수정 |
| 최종 검수 | 현재 Codex가 승인된 candidate를 read-only로 검수한다. | Grade 5 Codex room 또는 형님 지정 검수 | 외부 provider 결과만으로 acceptance/release 선언 |

외부 provider로 프로젝트 텍스트를 보내기 전에는 보낼 정확한 내용·목적·실무 효과를 형님께 설명하고 명시 승인을 받는다. provider에는 경로·URL·credential·셸·도구·attachment를 보내지 않으며, 반환 텍스트는 Codex가 현재 바이트와 대조한 뒤에만 사용한다.

## 3. Codex 검증 슬롯

아래 슬롯은 **검증 등급과 역할의 선택지**이지 7개 task/thread를 자동 생성하라는 의무가 아니다. 형님이 명시한 슬라이스에 필요한 슬롯만 사용한다.

| 슬롯 | 역할 | 등급 |
|---|---|---:|
| room-1 | 정적·문법·단일 smoke 테스트 | 1~2 |
| room-2 | unit·component·contract 테스트 | 3~4 |
| room-3 | PostgreSQL·복구·migration lifecycle 테스트 | 5 |
| room-4 | 빠른 read-only 정확성 검수 | 1~2 |
| room-5 | cross-layer·architecture·security 검수 | 3~4 |
| room-6 | adversarial·운영·복구 read-only 검수 | 5 |
| room-7 | room-6을 사용할 수 없을 때의 승인된 Grade 5 fallback | 5 |

- task/thread와 worktree는 형님이 명시하거나 Codex 앱이 승인한 경우에만 준비한다.
- 같은 슬라이스를 검증하는 방은 시작 전에 대상 파일·기준 SHA·status가 일치하는지 확인한다.
- 테스트방은 자기 worktree에서 직접 실행하고, 검수방은 파일을 수정하지 않는다.
- 기존 task/thread와 worktree를 삭제·덮어쓰기·reset하지 않는다. 완료 후 결과와 미검증 항목을 남긴다.

## 4. 슬라이스 실행 순서

1. 장소·오퍼레이터·구현 대상 Git 최상위를 확정한다.
2. 하나의 슬라이스에 목표, 읽기 범위, 쓰기 허용 경로, 완료조건을 한 번만 명시한다.
3. 외부 provider가 필요하면 정확한 텍스트와 목적을 형님께 제시하고 승인받은 뒤 Workbench 한 번만 호출한다.
4. provider 보고가 아니라 실제 diff·변경 경로·현재 바이트를 확인한다.
5. `OPERATOR=CODEX`이면 필요한 테스트 슬롯이 자기 worktree에서 직접 실행한다. `OPERATOR=CLAUDE_CODE`이면 Claude는 지정된 슬라이스의 read-only 검수만 수행한다.
6. 필요한 read-only 검수 등급을 선택하고 mutation-sensitive 증거와 미검증 범위를 함께 기록한다.
7. 최종 Grade 5는 Codex 또는 형님이 지정한 독립 검수로만 판정한다. Claude provider·Claude Code를 최종 acceptance 역할로 자동 승격하지 않는다.
8. 결과와 미검증 항목을 보고한다. Git 변경은 형님의 별도 요청이 있을 때만 수행한다.

FAIL이나 BLOCKED가 나오면 원인과 실제 변경 여부를 먼저 확인한다. 다른 provider·더 높은 등급·fallback을 자동으로 이어서 호출하지 않는다.

## 5. 프롬프트 작성 원칙

- 한 prompt에는 역할, 대상 범위, 완료조건, 금지사항, 출력 형식을 각각 한 번만 적는다.
- model·effort·fast는 현재 task/thread나 Workbench contract가 소유하면 prompt에 반복하지 않는다.
- provider prompt에는 repository path, URL, credential, shell, tool, attachment를 넣지 않는다.
- Codex 검증방 prompt에는 provider·wrapper·자식 Codex 재호출을 지시하지 않는다.
- 하네스·테스트 prompt는 목적·대상·성공조건·필수 금지사항만 짧고 명확하게 쓴다.
- 지시를 바꿀 때는 새 지시가 이전 지시를 대체한다고 명시한다.

## 6. 권한과 판정

- Writer는 승인된 텍스트 범위만 제안하고 저장소를 수정하지 않는다.
- 테스트 담당은 요구를 약화하거나 테스트를 삭제·skip·xfail해 PASS를 만들지 않는다.
- 검수 담당과 최종검수자는 read-only다.
- provider의 `DONE`보다 실제 diff, 완료조건, 테스트 결과를 우선한다.
- static PASS·외부 room 합계·보안 리뷰 PASS는 production acceptance나 release 승인과 다르다.
- 허용 범위 밖 변경, Git metadata 오염, 다른 장소 설정 변경을 발견하면 중단한다.

## 7. 보고 형식

실행 전에는 선택만 짧게 확정한다.

```text
ROUTE=<필요한 경우 장소-오퍼레이터-라이터>
REPOSITORY_ROOT=<본진 또는 승인된 구현 worktree>
SLICE=<검증 대상 슬라이스>
TEST=<등급과 직접 수행>
REVIEW=<등급과 read-only 수행>
GIT=MANUAL_ONLY
```

종료 시에는 실제 결과만 적는다.

```text
STATUS=PASS|FAIL|BLOCKED
CHANGED_PATHS=<실제 변경 경로>
TEST=<명령과 결과>
REVIEW=<판정과 근거>
FINAL=<Codex Grade 5 또는 형님 지정 검수>
GIT=NOT_REQUESTED|REQUESTED_RESULT
UNVERIFIED=<남은 항목>
```
