# SSWCenter 3.0 AI 오케스트레이션·검증 지침

> 지위: AI 역할, 호출 경계, 독립 작업방, 모델 배정, 프롬프트와 보고 정책의 정본
> 위치: `C:\sswcenter\3.0\00-오케스트레이션-작업지침.md`
> 적용 기준: 2026-08-12

이 문서는 **누가 누구를 호출하고 어디서 작업하는지**만 정한다. 장소·경로·인증·Git 경계는 [`00-먼저읽기-작업환경안내.md`](00-먼저읽기-작업환경안내.md), 등급의 의미는 [`docs/운영_오퍼레이터_등급_정의_v1.0.md`](docs/운영_오퍼레이터_등급_정의_v1.0.md), 실제 명령 형식은 [런타임 계약](.agents/skills/location-operator-writer/references/runtime-contracts.md)이 각각 소유한다. 다른 문서에는 이 내용을 복사하지 않고 링크만 둔다.

## 1. 작업 시작 선택

```text
장소 → 오퍼레이터 → 라이터 → 구현 작업공간 → 테스트 등급 → 검수 등급 → 실행
```

- 장소: `HOME` 또는 `OFFICE`
- 오퍼레이터: `CODEX` 또는 `CLAUDE_CODE`
- 라이터: `GROK` 또는 `DEEPSEEK`

호출명 `<장소>-<오퍼레이터>-<라이터>!`의 세 토큰이 선택을 확정한다. 누락되거나 모호한 값을 현재 PC, 과거 대화, 기본값으로 채우지 않는다.

## 2. 역할과 호출 경계

| 선택 | 수행 방식 | 허용된 AI 호출 | 금지 |
|---|---|---|---|
| `OPERATOR=CODEX` | Codex가 독립 task/thread와 worktree를 준비하고, 배정된 각 방의 Codex가 직접 테스트·검수한다. | Writer와 최종검수자 호출 | 본진 또는 독립방에서 `invoke-codex.ps1`, `codex exec`, 자식 Codex 재호출 |
| `OPERATOR=CLAUDE_CODE` | Claude Code가 작업을 관리하고 Codex에 테스트·검수를 위임한다. | `invoke-codex.ps1` | Codex 독립방 규칙을 자동 적용 |
| `WRITER=GROK` | 현재 오퍼레이터가 Grok에게 승인된 구현 범위만 맡긴다. | `invoke-grok.ps1` | 테스트·검수 담당으로 사용, 허용 경로 밖 쓰기 |
| `WRITER=DEEPSEEK` | 현재 오퍼레이터가 JSON Task Packet과 읽기·쓰기 허용 목록으로 구현을 맡긴다. | `invoke-deepseek-writer.ps1` | 자연어 prompt만 전달, 저장소 전체 쓰기 |
| 최종 독립검수 | 현재 오퍼레이터가 승인된 후보를 read-only로 검수시킨다. | `invoke-opus.ps1` 또는 승인된 room-7 fallback | 구현, 대화형 장기 수정 |

이 표가 프로젝트 작업의 호출 관계를 소유한다. 프로젝트 작업에서는 다음 두 문장에 예외가 없다.

> **Codex 오퍼레이터의 독립방은 이미 모델이 배정된 Codex 세션이므로, 그 방에서 Codex 래퍼나 Codex CLI를 다시 호출하지 않는다.**

> **`invoke-codex.ps1`는 Claude Code 오퍼레이터가 Codex에 등급별 테스트·검수를 위임할 때만 사용한다.**

오퍼레이터와 라이터는 별도 역할이다. 예를 들어 `사무실-코덱스-그록!`은 Codex가 운영하고 Grok이 승인 범위를 작성한다는 뜻이다. Grok 호출이 끝난 뒤 테스트·검수 방은 자기 worktree에서 직접 명령을 실행한다.

## 3. Codex 오퍼레이터의 독립 작업방

Codex 오퍼레이터는 현재 슬라이스의 검증을 위해 다음 7개 방을 사용한다. 각 행의 task/thread, Git worktree, 모델·effort·fast 배정은 **방을 만들 때 한 번에** 확정한다.

| 방 | 역할 | 등급 | 모델 | effort | fast |
|---|---|---:|---|---|---|
| room-1 | 테스트 | 1~2 | `gpt-5.3-codex-spark` | `xhigh` | off |
| room-2 | 테스트 | 3~4 | `gpt-5.6-luna` | `max` | on |
| room-3 | 테스트 | 5 | `gpt-5.6-sol` | `max` | off |
| room-4 | read-only 검수 | 1~2 | `gpt-5.6-luna` | `max` | on |
| room-5 | read-only 검수 | 3~4 | `gpt-5.6-sol` | `xhigh` | off |
| room-6 | read-only 검수 | 5 | `gpt-5.6-sol` | `ultra` | off |
| room-7 | Opus 불가 시 read-only 예비검수 | 5 | `gpt-5.6-sol` | `ultra` | off |

- room-1~7은 **검증방**이다. Writer 작업방이나 관리자방으로 바꾸지 않는다.
- 각 방은 서로 다른 Codex task/thread와 서로 다른 Git worktree를 사용한다.
- 모델은 task/thread 생성 시 배정한다. 방 안에서 래퍼로 모델을 다시 배정하지 않는다.
- 테스트방은 자기 worktree에서 필요한 테스트 명령이나 저장소 스크립트를 직접 실행한다.
- 검수방은 자기 worktree의 diff와 증거를 직접 읽고 판정하며 파일을 수정하지 않는다.
- 같은 후보를 검증하는 방은 시작 전에 대상 파일과 기준 상태가 일치하는지 확인한다.
- 기존 task/thread나 worktree를 삭제·덮어쓰기·reset하지 않는다.
- 완료한 task/thread는 결과와 미검증 항목을 남기고 보관한다. worktree 정리는 형님의 별도 지시가 있을 때만 한다.

Claude Code 오퍼레이터에는 이 7개 방을 자동 적용하지 않는다. Claude Code는 필요한 등급만 `invoke-codex.ps1`로 호출한다.

명시적 호출어 `안녕테스트해줘`의 공급자 연결 진단은 프로젝트 작업이 아니다. 이 진단의 Codex 단계는 Claude→Codex wrapper 진입점 자체를 한 번 확인할 뿐이며, 결과를 슬라이스 테스트·검수로 사용하지 않는다.

## 4. 슬라이스 실행 순서

1. 장소·오퍼레이터·라이터와 구현 대상 Git 최상위를 확정한다.
2. 목표, 읽기 범위, 쓰기 허용 경로, 완료조건을 한 번만 명시한다.
3. 선택한 Writer를 승인된 구현 작업공간에서 호출한다.
4. Writer 보고가 아니라 실제 diff와 변경 경로를 확인한다.
5. Codex 오퍼레이터이면 room-1~3의 배정 모델이 직접 테스트한다. Claude Code 오퍼레이터이면 필요한 테스트 등급을 `invoke-codex.ps1`로 호출한다.
6. Codex 오퍼레이터이면 room-4~6의 배정 모델이 직접 read-only 검수한다. Claude Code 오퍼레이터이면 필요한 검수 등급을 `invoke-codex.ps1`로 호출한다.
7. 필요한 경우 Opus 최종검수 또는 승인된 room-7 fallback을 수행한다.
8. 결과와 미검증 항목을 보고한다. Git 변경 작업은 형님의 별도 요청이 있을 때만 한다.

FAIL이나 BLOCKED가 나오면 원인과 실제 변경 여부를 먼저 확인한다. 다른 Writer, 더 높은 등급, fallback, 재호출을 자동으로 이어서 실행하지 않는다.

## 5. 프롬프트 작성 원칙

- 한 프롬프트에는 역할, 대상 Git 최상위, 읽기·쓰기 범위, 완료조건, 금지사항, 출력 형식을 각각 한 번만 적는다.
- 모델·effort·fast는 task/thread 생성이나 래퍼 등급에서 이미 정했으면 프롬프트에 반복하지 않는다.
- Codex 독립방 프롬프트에는 `invoke-codex.ps1`나 자식 Codex 실행 명령을 넣지 않는다.
- 지시를 바꿀 때는 새 지시가 이전 지시를 대체한다고 명시하고, 서로 충돌하는 보완 메시지를 연속으로 쌓지 않는다.
- **하네스 작성 프롬프트는 목적·대상·성공조건·필수 금지사항만 짧고 명확한 문장으로 쓴다. 구현 절차를 장황하게 대신 설계하지 않는다.**

예시:

```text
R0-A 마이그레이션의 적용과 롤백 결과를 격리 PostgreSQL에서 검증하는 PowerShell 하네스를 작성해. 제품 파일과 Git 상태는 변경하지 마.
```

## 6. 권한과 판정

- Writer는 승인된 쓰기 경로만 수정한다.
- 테스트 담당은 요구를 약화하거나 테스트를 삭제해 PASS를 만들지 않는다.
- 검수 담당과 최종검수자는 항상 read-only다.
- Writer의 `DONE`보다 실제 diff, 완료조건, 테스트 결과를 우선한다.
- 검수 FAIL을 구현 FAIL로 자동 해석하지 않는다.
- 허용 범위 밖 변경, Git metadata 오염, 다른 장소 설정 변경을 발견하면 중단한다.

환경·인증·Git의 상세 경계는 작업환경 안내를 따르며 이 문서에 복사하지 않는다.

## 7. 보고 형식

실행 전에는 선택만 짧게 확정한다.

```text
ROUTE=<장소-오퍼레이터-라이터>
REPOSITORY_ROOT=<본진 또는 승인된 구현 worktree>
ROOM=<해당 시 방 번호와 전용 worktree>
TEST=<등급과 직접 수행 또는 Claude→Codex 위임>
REVIEW=<등급과 직접 수행 또는 Claude→Codex 위임>
GIT=MANUAL_ONLY
```

종료 시에는 실제 결과만 적는다.

```text
STATUS=PASS|FAIL|BLOCKED
CHANGED_PATHS=<실제 변경 경로>
TEST=<명령과 결과>
REVIEW=<판정과 근거>
FINAL=<Opus 또는 명시된 fallback>
GIT=NOT_REQUESTED|REQUESTED_RESULT
UNVERIFIED=<남은 항목>
```
