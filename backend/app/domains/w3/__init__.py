"""Wave 3 deterministic domain policies.

The package starts with proposal-only policies. Persistence, parsing, matching,
apply commands, and public API wiring are deliberately separate later slices.
"""
