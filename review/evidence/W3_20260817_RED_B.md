# W3 RED-B — source-intake foundation

> 상태: `UNIT_RED_OBSERVED / UNIT_GREEN / HOST_FAILURES_RETAINED / SOL_ULTRA_FINAL_FAIL_RETAINED / SOL_FIX_HOST_GREEN_RETAINED / TERRA_REVIEW_FAIL_RETAINED / GROK_RESIDUAL_FIX_HOST_GREEN_RETAINED / TERRA_GROK_INDEPENDENT_REVIEW_FAIL_RETAINED / GROK_FIX3_HOST_GREEN / TERRA_P2_HOST_GREEN / CANDIDATE_MANIFEST_REGENERATED / INDEPENDENT_REVIEW_PENDING`

## 완료된 unit RED→GREEN

`backend/tests/test_w3_source_intake_unit.py`를 먼저 추가한 뒤 다음 RED를 확인했다.

```text
ModuleNotFoundError: No module named 'app.domains.w3.source_intake'
1 error during collection
```

`backend/app/domains/w3/source_intake.py`의 immutable classifier를 추가한 뒤
Grok REVIEW P2를 닫기 위해 same digest/profile 미지 상태 fail-open을 제거했다.

현재 unit 결과:

```text
test_w3_source_intake_unit.py: PASS
test_w3_wave_boundary_contract.py: PASS
```

분류 계약:

- 동일 digest/profile + 동등 성공 결과: `DUPLICATE_SUCCESS_NOOP`
- 동일 digest/profile + retryable `FAILED`: `RETRY_SAME_SNAPSHOT`
- 동일 digest/profile + `BLOCKED`: `BLOCKED_REUPLOAD_REJECTED`
- 동일 digest/new profile: `REPARSE_SAME_SNAPSHOT`
- different digest: `CANDIDATE_NEW_SNAPSHOT`
- unknown, `blocked`, `FAILED`, 공백, `FAILED_NONRETRYABLE`, `IN_PROGRESS`는
  ValueError로 fail-closed. `START_PROFILE_RUN` fallback 없음.
- 모든 허용 경로에서 새 receipt 보존, 업무 apply 0, supersede 0

## 0028 schema RED

필수 테스트 3개를 먼저 추가한 뒤 다음을 관찰했다.

```text
ModuleNotFoundError: No module named 'app.db.postcheck_current_0028'
3 errors during collection
```

이 RED를 PASS로 낮추지 않았다. 이후 0028 migration·model·postcheck·dispatcher·
readiness·grant·restore·W2 harness 역할 분리와 계약 테스트를 구현했다.

## GREEN (provider sandbox에서 검증한 범위)

```text
focused pytest: 95 passed, 11 skipped
11 skipped = test_w3_0028_postgres.py (SSWCENTER_W3_0028_REAL_PG != 1)
Ruff check PASS
Ruff format-check PASS
mypy app/domains/w3 + w3_models + postcheck_0028 + dispatch + readiness PASS
git diff --check clean
```

## Terra FIX static 재검증

CHECK name/lineage/ACL 교정 뒤 host PostgreSQL wrapper를 실행하지 않은 범위에서 다음을
재실행했다.

```text
focused pytest: 104 passed, 14 skipped
Ruff check + format-check: PASS
mypy W3 model/postcheck/dispatcher/source-intake/readiness: PASS
PowerShell parser (W3/W2/restore): PASS
git diff --check: clean
sample manifest + parent W2 delta current hashes: exact
```

14 skipped는 isolated W3 PostgreSQL harness flag가 없는 live PG nodes다. 이 static
결과는 host PostgreSQL GREEN 주장이 아니며 아래 host gate는 계속 남는다.

구현한 제품 seam:

- `backend/alembic/versions/20260817_0028_w3_source_intake_foundation.py`
  down_revision exact `20260817_0027_w2_official_card_assignee_and_plan_replacement`
- `backend/app/db/w3_models.py`
- `backend/app/db/postcheck_current_0028.py`
- dispatcher `ACTIVE_REVISION` exact 0028; HEAD marker는 0028만
- readiness exact 0028
- historical 0027 verifier는 `SSWCENTER_CURRENT_0027_DB_POSTCHECK_OK`만 유지
- W2 harness: lifecycle/restore DB exact 0027 + direct 0027 verifier;
  BrowserDatabase exact active 0028 + dispatcher

## Host PostgreSQL 첫 실행에서 관찰한 실패

첫 host 실행은 다음 lifecycle과 cleanup을 실제로 통과했다.

```text
0027 → 0028 → 0027 → 0028: PASS
application ACL applied: PASS
cleanup listener/process/temp/git_delta=0: PASS
```

그러나 current postcheck는 실패했다. SQLAlchemy naming convention이 raw CHECK 이름을
다시 prefix하여 실제 catalog가 예를 들어
`ck_w3_private_content_ck_w3_private_content_digest_sha256`가 되었고, postcheck가 기대한
clean name과 일치하지 않았다. 이 결과를 PostgreSQL GREEN으로 낮추지 않는다. 0028
migration은 `op.f("ck_...")`, ORM은 `conv("ck_...")`로 교정하고, 모든 expected CHECK의
정규화 exact 비교·append-only attempt ACL·receipt/run/snapshot composite lineage를 다시
검증하는 재실행이 필요하다.

## Host PostgreSQL 재실행에서 관찰한 두 번째 실패

CHECK 이름 교정 뒤의 재실행도 `0027 → 0028 → 0027 → 0028`, application ACL,
cleanup `listener/process/temp/git_delta=0`까지 통과했다. 그러나
`CURRENT_0028_CHECK_MISMATCH`에서 PostgreSQL 16의 안정된 deparse 표기가 migration
원문과 달랐다. `IN (...)`은 `= ANY (ARRAY[...])`, `NOT LIKE`는 `!~~`,
`position('://' in value)`는 `position(('://') in (value))`가 되었고 display-only
`::text` casts도 보였다.

이는 `CHECK(true)`나 열거값 축소를 허용하는 이유가 아니다. expected CHECK를 PG16
canonical form으로 고정하고, whitespace를 지우기 전에 `::text`만 제거해
`::text AND`가 `textAND`라는 가짜 type-name으로 합쳐지지 않게 교정했다. fake catalog는
10개 실제 deparse form을 허용하되, storage locator `http%` mutation, attempt status
array 축소, `CHECK(true)`, missing/extra CHECK를 계속 거부한다. 이 교정도 host wrapper
재실행 전까지 PostgreSQL GREEN이 아니다.

## Host W2 historical/current split에서 관찰한 실패

W2 host wrapper는 historical main DB의 exact 0027 lifecycle/direct postcheck를 통과한
뒤, `test_official_card_http_role_csrf_conflict_and_response_contracts`의 current FastAPI
GET `/eligible-assignees`에서 `503 NOT_READY`를 받았다. details reason은
`alembic_revision_mismatch`였고, 이 DB가 0027인 반면 production readiness는 active
0028을 요구하기 때문이다. cleanup은 `listener/process/temp/git_delta=0`이었다.

0027을 current로 속이거나 readiness bypass를 추가하지 않는다. historical main DB에서는
그 current-TestClient node만 explicit deselect한다. already-active-0028 BrowserDatabase의
dispatcher output은 `SSWCENTER_CURRENT_0028_DB_POSTCHECK_OK`와
`SSWCENTER_CURRENT_HEAD_POSTCHECK_OK`를 각각 exact 확인한다. 새 active test는
`/health/ready` 200 뒤 original W2 HTTP node의 모든 ADMIN/USER·CSRF·close-403·reassign
409/current-response assertions을 실제 FastAPI→service→PostgreSQL로 그대로 호출한다.
restore caller도 `SSWCENTER_CURRENT_0027_DB_POSTCHECK_OK` present와 current-head marker
absent를 generic `RESTORE_DRILL_OK`와 별도로 확인한다. 기존 real
FastAPI/Vite/Chromium reassign/409 browser gate도 동일 BrowserDatabase를 계속 사용한다.
이 분리는 host W2 wrapper 재실행 전까지 GREEN이 아니다.

## Host W2 rerun에서 관찰한 restore marker 실패

split 보정 뒤의 host W2 rerun은 historical PostgreSQL test `29 passed, 1 deselected`,
active-0028 BrowserDatabase dispatcher GREEN, original full HTTP contract `1 passed`, real
Chromium gate `1 passed`까지 통과했다. 그러나 마지막 restore caller는
`W2_0027_POSTGRES_RESTORE_HISTORICAL_0027_MARKER_MISSING`으로 실패했다. cleanup은 다시
`listener/process/temp/git_delta=0`이었다.

원인은 `restore-drill.ps1`의 outer revision gate가 historical 0025/0026/active-0028만
열고 historical 0027을 누락한 것이다. 따라서 안쪽 `postcheck_current_0027`/marker
검사는 작성돼 있어도 도달하지 못했고 generic `RESTORE_DRILL_OK`만 caller에 남았다.
outer gate에 `$Historical0027Revision`을 추가해 direct verifier와 caller-visible marker
출력을 실제 경로로 만들었다. current-head marker absent 검사는 유지한다. 이 minimal fix도
host W2 wrapper 재실행 전까지 GREEN이 아니다.

## Host PostgreSQL 최종 GREEN (main 재실행 증거)

위 네 번의 host 실패는 실패로 유지한다. 이후 main이 같은 wrapper를 재실행해 아래를
관찰했다. provider sandbox는 이 wrapper를 다시 실행하지 않았고, 아래 수치는 main이
공급한 최종 성공 증거다.

```text
W3 wrapper: 0027 → 0028 → 0027 → 0028
W3 live tests: 14 passed
W3 restore/live/seal: green
W3 stop: bounded
W3 cleanup listener/process/temp/git_delta: 0

W2 wrapper historical: 29 passed, 1 deselected
W2 active-0028 original HTTP: 1 passed
W2 real Chromium: 1 passed
W2 restore/live/seal: green
W2 cleanup listener/process/temp/git_delta: 0
```

```text
pwsh -NoProfile -File scripts/test-w3-0028-postgres-linux.ps1
pwsh -NoProfile -File scripts/test-w2-0027-postgres-linux.ps1
```

이 증거로 RED-B host 상태를 `HOST_GREEN`으로 올린다. 이후 Grok FIX가 추가한
quote-aware CHECK canonicalization과 W3 restore caller의
`SSWCENTER_CURRENT_0028_DB_POSTCHECK_OK` /
`SSWCENTER_CURRENT_HEAD_POSTCHECK_OK` 요구는 static 계약이며, 긴 host wrapper는
다시 실행하지 않는다.

## Sol Ultra 최종 REVIEW FAIL (2026-08-17)

위 HOST_GREEN을 취소하거나 이전 실패를 지울 근거는 없다. 그러나 Sol Ultra
최종 read-only 검수는 현재 바이트를 acceptance로 봉인하지 못했다.

P1:
- `_canonical_predicate`가 전역 `.lower()`라 quoted `'ACTIVE'`/`'active'`/
  `'Active'`를 같은 partial unique로 정규화했다.
- `w3_source_snapshot`/`w3_import_run`에 table-wide UPDATE가 남아 있고
  immutable W3 표도 열 단위 ACL을 검사하지 않았다.
- CHECK 계약이 constraint name만 보고 table+definition+cardinality에
  묶이지 않아 moved/duplicate를 놓칠 수 있다.
- `has_table_privilege`만으로는 예기치 않은 열 GRANT를 거부하지 못한다.

P2:
- 여섯 표의 exact type/nullability/default/identity, PK, explicit
  non-constraint index 계약이 없다.
- 직접 0028 revision verifier가 `alembic_version` 전체 행이 정확히
  `[EXPECTED_REVISION]`인지 보지 않아 rogue second head가 통과할 수 있다.
- race oracle이 모든 Exception을 reject로 접어 unexpected 실패를 숨긴다.

P3:
- 0028 migration Ruff format drift.
- current plan stale `96/98 + 2` (실제 parent arithmetic은 `85/98 + 13`).

이 절 이후 상태는 `FIX_REQUIRED / RETEST_REQUIRED`다. 아래 Grok FIX는
해당 finding만 고치고 static 계약 테스트를 추가한다. 긴 host W3/W2
wrapper는 실행하지 않으며, live PostgreSQL GREEN은 main 재실행 전까지
주장하지 않는다.

## Grok FIX static 재검증 (Sol Ultra finding 반영, live 미실행)

반영한 제품 seam:

- `_canonical_predicate`가 CHECK와 같은 quote-aware unquoted fold를 쓴다.
  fake catalog는 `'ACTIVE'`만 허용하고 `active`/`Active`를 거부한다.
- 여섯 W3 표 table-wide UPDATE 회수, snapshot/run만 `status` 열 UPDATE.
  postcheck는 relacl UPDATE=false, 모든 검토 열 `has_column_privilege`,
  예기치 않은 attacl GRANT 거부를 요구한다.
- CHECK 키는 `(table, name)` + cardinality + exact definition.
- 여섯 표 exact type/null/default/identity, PK, explicit index.
- 직접 0028 verifier가 `alembic_version` 전체 행 `== [EXPECTED_REVISION]`.
- race oracle은 IntegrityError + 23505 + exact partial unique name만 접는다.

```text
focused pytest: 84 passed, 18 skipped
18 skipped = test_w3_0028_postgres.py live nodes (SSWCENTER_W3_0028_REAL_PG != 1)
Ruff check PASS
Ruff format-check PASS (0028 migration included)
mypy postcheck_0028 + dispatch + w3_models + domains/w3 + readiness PASS
git diff --check: clean
parent W2 delta: 85/98 + 13, unauthorized 0
PowerShell AST parse: NOT RUN (pwsh absent in this sandbox)
ensure-runtime / verify-runtime: NOT RUN (pwsh absent)
host W3/W2 wrappers: NOT RUN (main rerun required)
```

이 static 결과는 live ACL/predicate/revision/race/catalog mutation GREEN이
아니다. 상태는 계속 `FIX_REQUIRED / RETEST_REQUIRED`다.

## Sol finding FIX 이후 host 재검증

main의 첫 재실행은 제품 DB를 열기 전에 W3 wrapper의 exact node drift gate에서
`W3_0028_POSTGRES_NODE_DRIFT`로 실패했다. live test가 14개에서 18개로 늘었지만
wrapper node 배열이 갱신되지 않은 것이 원인이었다. cluster 시작 전 실패라 새
listener/process/temp는 없었다. 배열을 실제 test 함수 순서와 exact 동기화했다.

두 번째 재실행은 lifecycle과 ACL 적용 뒤 current postcheck에서
`CURRENT_0028_TABLE_RELACL_MISMATCH`로 실패했다. relacl exact 비교가 검토 대상이 아닌
정상 table owner `erp_owner`의 권한까지 포함한 것이 원인이었다. owner만 제외하고
PUBLIC·erp_app·erp_backup·기타 예상 밖 grantee는 계속 검사하도록
`acl.grantee <> relation_row.relowner`를 고정했다. cleanup은
`listener/process/temp/git_delta=0`이었다.

세 번째 재실행은 18개 live node 중 `17 passed, 1 failed`였다. 새 status-only ACL
테스트가 앞선 append-only test가 커밋한 `DIGEST_C`를 재사용해
`uq_w3_private_content_content_digest`에 걸린 fixture isolation 오류였다. ACL 제품
동작의 실패로 낮추지 않고 전용 `DIGEST_D`로 분리했다. cleanup은 다시
`listener/process/temp/git_delta=0`이었다.

그 뒤 main이 현재 바이트에서 두 host wrapper를 순차 재실행해 다음을 확인했다.

```text
W3 wrapper: 0027 → 0028 → 0027 → 0028
W3 live tests: 18 passed
W3 restore/live/seal: green
W3 stop: bounded
W3 cleanup listener/process/temp/git_delta: 0

W2 wrapper historical: 29 passed, 1 deselected
W2 active-0028 original HTTP: 1 passed
W2 real Chromium: 1 passed
W2 restore/live/seal: green
W2 cleanup listener/process/temp/git_delta: 0
```

이 결과는 Sol finding 수정의 host 재검증을 닫지만 최종 봉인은 아니다. candidate
manifest를 현재 바이트로 재생성하고 Sol Ultra read-only 최종검수를 처음부터 다시
통과해야 한다.

## Terra 독립 REVIEW FAIL (2026-08-17)

위 SOL_FIX_HOST_GREEN을 취소하거나 이전 실패를 지울 근거는 없다. Terra 독립
read-only 검수는 현재 바이트를 봉인하지 못했다.

Residual 1 P1: `backend/app/db/postcheck_current_0028.py`가 relation owner ACL을
제외하면서 여섯 W3 table과 대응 identity sequence owner가 정확히 `erp_owner`인지,
identity sequence가 해당 table `id`에 `OWNED BY`인지 catalog exact 검사하지 않는다.
owner 검증 없이 owner ACL을 제외하면 unexpected owner를 놓친다.

Residual 2 P2: W3 identity sequence `relacl`과 공유 `erp` schema ACL에서
PUBLIC/third-party/grant-option/unexpected grantor를 전수 검사하지 않는다.
기존 wave의 합법 grant는 schema USAGE=`erp_app`+`erp_backup`과 sequence
`erp_app=USAGE+SELECT`, `erp_backup=SELECT`다. table/column status-only ACL
closure는 유지해야 한다.

Residual 3 P2: PK catalog-equivalence에 `condeferrable=false` /
`condeferred=false`가 없고, constraint-backed index와 승인된 ACTIVE partial
unique를 별도 처리한 뒤 모든 non-constraint W3 index를 name/table/access
method/key/INCLUDE/unique/predicate/validity로 전수 비교하지 않는다. extra
partial/expression index와 hash replacement를 거부하지 못한다. quoted
`ACTIVE`와 exact CHECK는 약화하면 안 된다.

이 절 이후 상태는 `FIX_REQUIRED / RETEST_REQUIRED`다. host live wrapper GREEN과
최종 PASS는 선언하지 않는다.

## Grok FIX (Terra residual 3건, live wrapper 미실행)

반영한 제품 seam:

- 0028 upgrade가 여섯 table에 명시 `ALTER TABLE erp.<table> OWNER TO erp_owner`.
  identity sequence는 table owner를 따른다. identity sequence에 대한
  `ALTER SEQUENCE OWNER TO`는 PG가 SQLSTATE `0A000`으로 거부하는 관례를 유지한다.
- postcheck는 `_verify_relation_owners`로 table owner, sequence owner, serial
  sequence name, `pg_depend.deptype='i'`, owned column=`id`를 exact 검사한 뒤에만
  기존 table owner ACL 제외를 적용한다.
- 각 W3 identity sequence `relacl`과 공유 `erp` schema `nspacl`을 `aclexplode`
  exact allowlist로 검사한다. verified owner 외 sequence는
  `erp_app=USAGE+SELECT`, `erp_backup=SELECT`; schema는 `erp_app=USAGE`,
  `erp_backup=USAGE`. PUBLIC/third-party/grant-option/unexpected grantor는
  fail-closed. 기존 table/column status-only ACL은 보존한다.
- PK는 `condeferrable=false`, `condeferred=false`를 exact 검사한다.
  constraint-backed unique/PK는 기존 verifier가 담당하고, 승인 ACTIVE partial
  unique는 기존 quote-aware verifier를 유지한다. 모든 non-constraint W3 index는
  name/table/access method/key/INCLUDE/unique/predicate/validity를 전수 비교한다.
- unexpected-owner, sequence/schema ACL, deferrable PK, hash replacement,
  extra partial/expression index의 fake catalog와 live mutation test를 추가했다.
  W3 live node는 18 → 21로 harness allowlist와 exact 동기화했다.

중간 실패 (실패로 보존):

1. Ruff format-check 2파일 (`test_w3_0028_postcheck_unit.py`,
   `test_w3_0028_postgres.py`) unformatted. mechanical format 후 PASS.
2. contract `OWNER TO erp_owner` count가 loop f-string 때문에 1이었다. 여섯
   명시 `ALTER TABLE`로 고친 뒤 주석의 `ALTER SEQUENCE` 문자열이 executable
   부재 검사와 다시 충돌했다. executable-only 검사로 고쳤다.

```text
focused pytest: 76 passed, 21 skipped
21 skipped = test_w3_0028_postgres.py live nodes (SSWCENTER_W3_0028_REAL_PG != 1)
Ruff check PASS
Ruff format-check PASS
mypy postcheck_0028 + dispatch + w3_models + domains/w3 + readiness PASS
git diff --check: clean
PowerShell AST parse: NOT RUN (pwsh absent in this sandbox)
ensure-runtime / verify-runtime: NOT RUN (pwsh absent)
host W3/W2 wrappers: NOT RUN (main rerun required)
```

이 static 결과는 live owner/ACL/PK/index mutation GREEN이 아니다. 상태는 계속
`FIX_REQUIRED / RETEST_REQUIRED`다. stale
`review/evidence/W3_20260817_CURRENT_CANDIDATE_MANIFEST.sha256`는 수정하지 않았다.

## Grok residual FIX 이후 Codex host 재검증 (2026-08-18)

Codex host의 첫 focused pytest 호출은 다른 정적 gate와 병렬 실행 중 pytest capture
임시파일이 사라져 `FileNotFoundError`로 exit 1, `no tests ran`이었다. 이 결과를 PASS로
낮추지 않았다. 같은 current bytes를 capture 비활성 `-s`로 직렬 재실행해 다음을
확인했다.

```text
focused pytest -s: 76 passed, 21 skipped
21 skipped = isolated live PostgreSQL nodes
Ruff check + format-check: PASS
mypy: PASS
PowerShell AST (W3/W2/restore): PASS
git diff --check: clean
```

그 뒤 두 host wrapper를 순차 실행했다.

```text
W3 wrapper: 0027 -> 0028 -> 0027 -> 0028
W3 live tests: 21 passed
W3 restore/live/seal: green
W3 stop: bounded
W3 cleanup listener/process/temp/git_delta: 0

W2 wrapper historical: 29 passed, 1 deselected
W2 active-0028 original HTTP: 1 passed
W2 real Chromium: 1 passed
W2 restore/live/seal: green
W2 cleanup listener/process/temp/git_delta: 0
```

이 실행은 Terra residual 3건의 host 재검증을 닫는다. 최종 봉인은 아니며 Grok/Terra
독립 read-only 재검수, current candidate manifest 재생성, Sol Ultra 최종검수가 남는다.

## Terra·Grok 독립 REVIEW FAIL (2026-08-18)

위 Grok residual FIX의 host 재검증 수치를 지우거나 이전 실패를 취소하는 근거는 없다.
그러나 독립 read-only 검수는 현재 바이트를 봉인하지 못했고, 이 절 이후 현재 상태는
`FIX_REQUIRED / RETEST_REQUIRED`다. `HOST_GREEN`은 현재 최종 상태가 아니다.

Finding A P2, Terra: `_acl_non_owner_drifts`가 W3 identity sequence와 공유 `erp`
schema의 owner-self ACL 일반 권한 집합을 검사하지 않는다. 부분적이거나 적대적인
non-grantable owner row가 false-green할 수 있다. 암시 owner ACL은 없거나, 물질화되면
PG16 canonical 전체 집합이어야 하며 grantor=`erp_owner`, grantable=false여야 한다.
sequence owner 정본은 SELECT, UPDATE, USAGE. schema owner 정본은 CREATE, USAGE.
부분/잉여 owner set은 실패여야 한다.

Finding B P2, Grok: W3 table `relacl`과 column `attacl`이 grantor를 보지 않는다.
table non-owner 정본은 그대로 `erp_app` SELECT+INSERT, `erp_backup` SELECT이며 모두
grantor=`erp_owner`, grant option 없음. owner row는 없거나 PG16 table owner 전체
집합(SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER)만 허용.
column은 `snapshot.status`와 `import_run.status`만 `erp_app` UPDATE(from owner,
no grant option)를 허용하고 나머지 non-owner column ACL은 비어야 한다. 물질화된
owner column ACL은 없거나 PG16 column owner 전체(SELECT, INSERT, UPDATE, REFERENCES)
만 허용. status-only 유효권한 검사는 약화하면 안 된다.

Finding C P3, Grok: `test_w3_0028_pg_receipt_row_and_attempt_are_append_only_for_erp_app`가
SQLSTATE 없이 ProgrammingError 또는 IntegrityError를 받는다. receipt DELETE가 허용된 뒤
FK 23503만으로 실패해도 통과할 수 있다. 이 permission oracle의 모든 문은 SQLSTATE
42501이어야 하며 23503이나 무관한 예외를 받으면 안 된다.

## Grok FIX (Terra A + Grok B/C, live wrapper 미실행)

반영한 제품 seam:

- `_acl_non_owner_drifts`를 `_exact_acl_drifts`로 교체했다. owner row는 없거나
  객체 종류별 PG16 canonical 전체 집합(grantor=owner, non-grantable)만 허용하고
  부분/잉여 owner set은 fail-closed다. sequence/schema/table/column raw ACL이
  같은 helper를 재사용한다.
- table `relacl`은 owner를 제외하지 않고 grantor를 포함해 전수 비교한다.
  `acl.grantee <> relation_row.relowner` 필터를 제거했다. non-owner exact set과
  table-wide UPDATE 거부는 유지한다.
- 모든 검토 열 `attacl`을 grantor 포함으로 전수 비교한다. status 열만
  `erp_app` UPDATE(from owner, no grant option)를 허용한다. 유효권한
  `has_table_privilege`/`has_column_privilege` 검사는 유지한다.
- append-only permission oracle은 모든 문에 SQLSTATE 42501만 받는다.
  child row는 유지해 privilege 검사가 FK보다 먼저 이기도록 한다.
- fake-catalog: 부분/잉여 owner set, 동일 grantee/privilege의 unexpected grantor,
  완전한 owner set 허용을 추가했다.
- live savepoint: sequence `REVOKE UPDATE FROM erp_owner`, schema
  `REVOKE CREATE FROM erp_owner`, table `REVOKE TRIGGER FROM erp_owner`를
  실제 catalog drift와 함께 검사한다. table/column unexpected grantor는 제3
  role + GRANT OPTION 경로를 쓰고, 최종 catalog의 grantor가 그 role인지
  확인하며 postgres grantor를 주장하지 않는다.
- W3 live node는 21 → 22로 harness allowlist와 exact 동기화했다.

중간 실패 (실패로 보존):

1. 첫 Ruff check는 leftover `_acl_non_owner_drifts` 참조(F821)와 E501 2건,
   format-check 2파일이 unformatted였다. leftover를 `_exact_acl_drifts`로 고치고
   mechanical format 후 같은 scoped Ruff check/format-check가 PASS했다.
2. 이 sandbox에는 `pwsh`가 없다. `scripts/ensure-runtime.ps1` /
   `scripts/verify-runtime.ps1`와 PowerShell AST는 실행하지 못했고
   `SSWCENTER_RUNTIME_GREEN`을 선언하지 않는다. Python 3.12.3과 ignored
   `backend/.venv`만 사용했다.

```text
focused pytest (0028 contract/unit/postgres + source-intake + wave-boundary):
  70 passed, 22 skipped
broader focused (+ plan-adjustment + W2 current HTTP + schema contract):
  82 passed, 23 skipped, 1 preexisting SAWarning
22 skipped = test_w3_0028_postgres.py live nodes (SSWCENTER_W3_0028_REAL_PG != 1)
1 additional skip = test_w3_0028_w2_current_http_postgres.py harness flag
Ruff check PASS
Ruff format-check PASS
mypy postcheck_0028 + dispatch + w3_models + domains/w3 + readiness PASS
git diff --check: clean
W3 live node list/count/order: 22, harness AST drift check OK
PowerShell AST parse: NOT RUN (pwsh absent in this sandbox)
ensure-runtime / verify-runtime: NOT RUN (pwsh absent)
SSWCENTER_RUNTIME_GREEN: NOT CLAIMED
host W3/W2 wrappers: NOT RUN (Codex host rerun required)
```

이 static 결과는 live owner/ACL grantor/42501 mutation GREEN이 아니다. 상태는
`FIX_REQUIRED / RETEST_REQUIRED`다. stale
`review/evidence/W3_20260817_CURRENT_CANDIDATE_MANIFEST.sha256`와 네 W2 통제
유물은 수정하지 않았다. 현재 host GREEN이나 최종 PASS를 선언하지 않는다.

## Codex host 재실행에서 관찰한 nullable ACL 실패 (2026-08-18)

exact ACL patch 뒤 Codex host가 W3 wrapper를 재실행했다. 아래는 실패로 유지한다.

```text
0027 → 0028 → 0027 → 0028: PASS
application ACL applied: PASS
W3 current postcheck: FAIL before live nodes
bounded stop: PASS
cleanup listener/process/temp/git_delta: 0
```

실패 원문:

```text
psycopg.errors.InvalidParameterValue: ACL arrays must be one-dimensional
backend/app/db/postcheck_current_0028.py `_verify_w3_column_attacl_entries`
LEFT JOIN LATERAL aclexplode(COALESCE(attribute_row.attacl, {}::aclitem[])) AS acl ON true
```

NULL `attacl`을 `'{}'::aclitem[]`로 치환하면 PostgreSQL 16에서 0차원 빈 배열이
되어 `aclexplode`가 거부한다. 이 결과를 host GREEN으로 낮추지 않는다.

## Grok FIX (nullable ACL expansion, live wrapper 미실행)

반영한 제품 seam:

- column/table/sequence/schema ACL 질의가
  `LEFT JOIN LATERAL aclexplode(<acl_column>) AS acl ON true`를 직접 호출한다.
  STRICT SRF는 NULL에서 행을 만들지 않고, LEFT JOIN이 바깥 객체 행을 유지한다.
- `'{}'::aclitem[]` / `ARRAY[]::aclitem[]` COALESCE를 쓰지 않는다. NULL attacl
  열을 건너뛰거나 per-column coverage를 약화하지 않는다.
- column owner live mutation GRANT를
  `SELECT (status), INSERT (status), UPDATE (status), REFERENCES (status)`로
  고친다. 기존 `GRANT SELECT, INSERT, UPDATE, REFERENCES (status)`는
  열 단위 owner 집합을 물질화하지 못한다.
- fake/catalog 계약이 위 SQL 형태와 NULL attacl 행 전수 커버를 고정한다.
- W3 live node 22개 이름·순서와 harness allowlist는 그대로다.

중간 실패 (실패로 보존):

1. 이 sandbox에는 `pwsh`가 없다. `scripts/ensure-runtime.ps1` /
   `scripts/verify-runtime.ps1`와 PowerShell AST는 실행하지 못했고
   `SSWCENTER_RUNTIME_GREEN`을 선언하지 않는다. Python 3.12.3과 ignored
   `backend/.venv`만 사용했다.

```text
focused pytest (0028 contract/unit/postgres + source-intake + wave-boundary):
  71 passed, 22 skipped
broader focused (+ plan-adjustment + W2 current HTTP + schema contract):
  83 passed, 23 skipped, 1 preexisting SAWarning
22 skipped = test_w3_0028_postgres.py live nodes (SSWCENTER_W3_0028_REAL_PG != 1)
1 additional skip = test_w3_0028_w2_current_http_postgres.py harness flag
Ruff check PASS
Ruff format-check PASS
mypy postcheck_0028 + dispatch + w3_models + domains/w3 + readiness PASS
git diff --check: clean
W3 live node list/count/order: 22, harness allowlist exact
PowerShell AST parse: NOT RUN (pwsh absent in this sandbox)
ensure-runtime / verify-runtime: NOT RUN (pwsh absent)
SSWCENTER_RUNTIME_GREEN: NOT CLAIMED
host W3/W2 wrappers: NOT RUN (Codex host rerun required)
```

이 static 결과는 live nullable-ACL / column-owner GRANT mutation GREEN이 아니다.
상태는 `FIX_REQUIRED / RETEST_REQUIRED`다. stale
`review/evidence/W3_20260817_CURRENT_CANDIDATE_MANIFEST.sha256`와 네 W2 통제
유물은 수정하지 않았다. 현재 host GREEN이나 최종 PASS를 선언하지 않는다.

## Grok FIX-3 이후 Codex host 재검증 (2026-08-18)

Codex host에서 runtime GREEN과 PowerShell AST를 확인한 뒤 현재 바이트를 재실행했다.

```text
static: 83 passed, 22 live-PG skipped
Ruff check + format-check: PASS
mypy: PASS
PowerShell AST: PASS
git diff --check: clean

W3 wrapper: 0027 -> 0028 -> 0027 -> 0028
W3 live tests: 22 passed
W3 restore/live/seal: green
W3 stop: bounded
W3 cleanup listener/process/temp/git_delta: 0

W2 wrapper historical: 29 passed, 1 deselected
W2 active-0028 original HTTP: 1 passed
W2 real Chromium: 1 passed
W2 restore/live/seal: green
W2 cleanup listener/process/temp/git_delta: 0
```

따라서 nullable ACL, owner partial set, table/column grantor, append-only 42501의 host
재검증은 닫혔다. 최종 봉인은 아니며 current candidate manifest 재생성, 독립 Grok/Terra
read-only 재검수, Sol Ultra 최종검수가 남는다.

## Current candidate manifest 재생성

첫 `apply_patch`는 같은 경로를 한 patch 안에서 delete+add하려 해 도구가 거부했고
manifest 바이트는 바뀌지 않았다. 기존 전체 내용을 `Update File` diff로 교체해 다시
적용했다. plan/evidence ledger 반영 뒤 동일 140-path scope를 최종 재생성하고 다음을
독립 검수 시작조건으로 둔다.

```text
entries=140
status/hash/bytes mismatch=0
extras=0
missing=0
excluded=current manifest itself + untracked .codex + post-review controls
```

## Terra post-FIX3 독립검수와 P2 수정 (2026-08-18)

Terra 독립 read-only 검수는 기능 로직 외에 두 catalog false-green과 진행표 오기를
발견했다.

1. current 0028 table inventory가 `information_schema.tables`를 사용해 app role에
   권한 없는 rogue `erp.w3_*` relation을 숨길 수 있었다. `pg_class`와
   `pg_namespace`, exact relkind `r/p/v/f`로 바꾸고 fake catalog 및 isolated
   PostgreSQL erp_app dispatcher mutation을 추가했다. 새 W3 live allowlist는 23개다.
2. historical 0027 direct verifier가 scalar 첫 Alembic revision만 읽었다. 전체 행이
   정확히 기대 0027 하나일 때만 통과하게 하고 W2 harness 안에 rogue second-head,
   양 marker 부재, 정리 후 sole-head 복구 oracle을 추가했다.
3. current plan의 RED-B 행이 완료된 FIX-3 host 재실행을 아직 미실행이라고 적은 P3는
   과거 실패 ledger를 보존한 채 현재 상태와 분리해 바로잡았다.

수정 actor 범위의 결과는 다음과 같다.

```text
hidden W3 relation isolated PostgreSQL: 1 passed
W3 fake/unit: 24 passed
W2 revision + W3 postcheck unit: 26 passed
Ruff check/format: PASS
mypy 0027/0028 postchecks: PASS
PowerShell AST: PASS
isolated listener/temp cleanup: 0
```

합산 focused pytest의 첫 실행은 parent W2 delta에 새 0027 postcheck와 W2 harness의
current SHA/bytes가 반영되지 않아 `1 failed, 83 passed, 24 skipped`였다. 이 실패는
숨기지 않고 parent delta 두 intentional row를 current bytes로 갱신한 뒤 재실행한다.
전체 W3/W2 host wrapper는 아직 재실행 전이므로 현재 최종 PASS나 봉인을 선언하지 않는다.

## Terra P2 수정 뒤 host 재검증 (2026-08-18)

parent W2 delta 두 row를 먼저 current bytes로 갱신한 뒤 합산 focused gate는
`84 passed, 24 expected skipped`로 녹색이었다. 이어 W3 host는 다음을 통과했다.

```text
W3 lifecycle: 0027 -> 0028 -> 0027 -> 0028
W3 live PostgreSQL: 23 passed
hidden no-privilege W3 relation: rejected by erp_app dispatcher
W3 restore/live/seal: GREEN
W3 bounded stop: GREEN
W3 cleanup listener/process/temp/git_delta: 0
```

첫 W2 host는 새 rogue-second-head를 거부한 뒤 cleanup sole-head query가 한 문자열을
반환했을 때 `$RestoredRevisions.Count`가 없는 StrictMode 오류로 실패했다. 이 실행도
cleanup listener/process/temp/git_delta는 0이었다. Grok xhigh FIX 호출은
`max turns reached`로 status failed였으나 종료 직전 pipeline 전체를 `@(...)`로 감싸는
patch와 static contract를 남겼다. provider 결과를 PASS로 세지 않고 Codex가 현재
바이트의 단위 2 passed, Ruff, PowerShell AST를 확인했다.

수정 뒤 W2 host를 처음부터 재실행한 결과는 다음과 같다.

```text
historical rogue second head: REJECTED; both success markers absent
historical W2 PostgreSQL: 29 passed, 1 deselected
active 0028 original W2 HTTP: 1 passed
real Chromium reassignment/409: 1 passed
W2 restore/live/seal: GREEN
W2 cleanup listener/process/temp/git_delta: 0
```

따라서 두 Terra P2와 그 검증 oracle의 scalar 오류는 host에서 닫혔다. 현재 바이트를
candidate manifest로 다시 동결하고 Grok/Terra 독립 review와 Sol Ultra final review를
새로 시작한다. 이 단계 자체는 최종 사용자 봉인이 아니다.

manifest 재생성 뒤 첫 exact verifier는 `/^[ #]/!p`로 선언 경로를 추출해 앞 status가
공백인 tracked-modified 58개 row도 누락했고 `extras=58`을 잘못 냈다. 이는 candidate
drift가 아니라 verifier 필터 오류다. 헤더 `#`만 제외하도록 바로잡은 재검사는
141 entries, status/hash/bytes mismatch 0, extras 0, missing 0이다. 이 문제 기록까지
포함한 current bytes로 manifest를 다시 생성하고 독립 review를 시작한다.
